# Web Framework
Flask==3.0.0
Flask-Cors==4.0.0

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# AI API (using REST API directly - no google-generativeai library needed)
# Note: We use requests (built-in dependency of Flask) for Gemini API calls

# Google Sheets
gspread==6.1.0
oauth2client==4.1.3

# Utilities
python-dotenv==1.0.0

# Async support (for link validation)
aiohttp==3.9.1
